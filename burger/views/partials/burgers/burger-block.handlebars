<li>
	{{name}}

	<button class="devour" data-id="{{id}}" data-justdevoured="{{devoured}}">
		{{#if devoured}}Devour It?{{else}}Order another?{{/if}}
	</button>
		<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>

</li>
