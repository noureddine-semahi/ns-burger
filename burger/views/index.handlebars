<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/4.1.3/css/bootstrap.min.css">

<script src="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/4.1.3/js/bootstrap.min.js"></script>
<link rel="stylesheet" href="/assets/css/burger_style.css" type="text/css"/>



<body>
  

  <div class="row">
    <div class="col-12">
      <h1>Eat-Da-Burger</h1>
    </div>
  </div>

  <div class="container-fluid">
    <div class="row">
      <div class="col-md-12 col-sm-12 col-xs-12">
        
        <div class="col-md-6 col-sm-6 col-xs-6"style="color:white">
          <h2>Burgers as yet undevoured. . .</h2>
            <ul>
              {{#each burgers}}
                {{#unless devoured}}
                  {{> burgers/burger-block devoured=true}}
                {{/unless}}
              {{/each}}
            </ul>
        </div>
             
        <div class="col-md-6 col-sm-6 col-xs-6"style="color:white">
          <h2>Burgers that have been DEVOURED.</h2> 
            <ul>
              {{#each burgers}}
                {{#if devoured}}
                  {{> burgers/burger-block devoured=false}}
                {{/if}}
              {{/each}}
            </ul>
        </div>    
      </div>
    </div>
  </div>

  <div class="container-fluid">
    <div class="row">
      <div class="col-12">
        <h2>Add a Burger</h2>
        <form class="create-form">

            <div class="form-group">
                <label for="bn">Burger Name:</label>
                <input type="text" id="bn" name="name">
            </div>
            <div style="color:white" class="form-group">
                <label  style="color:white"for="devour"> Has Das Burger Been Devoured?</label><br>
                <input style="color:white" type="radio" name="devoured" value="1" checked> Devoured!<br>
                <input  style="color:white" type="radio" name="devoured" value="0"> Still up for grabs. . .
                <button type="submit">Add burger</button>
            </div>
        </form>
      </div>
    </div>
  </div>
</body>
</html>